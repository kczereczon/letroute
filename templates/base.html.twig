<!DOCTYPE html>
<html class="h-full">
<head>
    <meta charset="UTF-8">
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <title>{% block title %}Letroute - narzędzie do planowania tras{% endblock %}</title>

    <link rel="icon"
          href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}

    <style>
        html, body {
            height: 100vh;
            margin: 0;
            padding: 0;
            overflow: hidden;
        }

        .full-height {
            height: calc(100vh - 20px); /* Subtract the height of the element with class "h-10" */
        }

        .full-height-with-padding {
            height: calc(100vh - 20px - 3rem - 88px);
        }
    </style>
</head>
</head>
<body>
<!--
  This example requires updating your template:

  ```
  <html class="h-full bg-gray-100">
  <body class="h-full">
  ```
-->
<body>
    <nav class="flex items-center justify-between flex-wrap bg-teal-500 p-6 h-20">
        <div class="flex items-center flex-shrink-0 text-white mr-6">
            <svg class="fill-current h-8 w-8 mr-2" width="54" height="54" viewBox="0 0 54 54" xmlns="http://www.w3.org/2000/svg"><path d="M13.5 22.1c1.8-7.2 6.3-10.8 13.5-10.8 10.8 0 12.15 8.1 17.55 9.45 3.6.9 6.75-.45 9.45-4.05-1.8 7.2-6.3 10.8-13.5 10.8-10.8 0-12.15-8.1-17.55-9.45-3.6-.9-6.75.45-9.45 4.05zM0 38.3c1.8-7.2 6.3-10.8 13.5-10.8 10.8 0 12.15 8.1 17.55 9.45 3.6.9 6.75-.45 9.45-4.05-1.8 7.2-6.3 10.8-13.5 10.8-10.8 0-12.15-8.1-17.55-9.45-3.6-.9-6.75.45-9.45 4.05z"/></svg>
            <span class="font-semibold text-xl tracking-tight">Letroute</span>
        </div>
        <div class="block lg:hidden">
            <button class="flex items-center px-3 py-2 border rounded text-teal-200 border-teal-400 hover:text-white hover:border-white">
                <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/></svg>
            </button>
        </div>
        <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">
            <div class="text-sm lg:flex-grow">
                <form class="block lg:inline-block" id="create" action="{{ path('app_set_create') }}" method="post" enctype="multipart/form-data">
                    <input type="file" name="points" id="points" hidden>
                    <a id="submit" type="submit"
                       class="inline-block text-sm px-4 py-2 leading-none border rounded text-white border-white hover:border-transparent hover:text-teal-500 hover:bg-white mt-4 lg:mt-0">UTWÓRZ NOWY ZBIÓR</a>
                </form>
                {% if setId %}
                    <form class="block lg:inline-block" action="{{ path('app_generate_routes', {'id': setId}) }}" method="POST">
                        <button class="inline-block text-sm px-4 py-2 leading-none border rounded text-white border-white hover:border-transparent hover:text-teal-500 hover:bg-white mt-4 lg:mt-0">
                            WYGENERUJ TRASY
                        </button>
                    </form>
                {% endif %}
            </div>
        </div>
    </nav>
<div class="flex min-h-full">
    {% block body %}{% endblock %}
</div>
</body>

<script>
    let form = document.getElementById('create');
    let file = document.getElementById('points');
    let submit = document.getElementById('submit');

    submit.addEventListener('click', (event) => {
        event.preventDefault();
        file.click();
    })

    file.addEventListener('change', (event) => {
        form.submit();
    })
</script>
</body>
</html>
